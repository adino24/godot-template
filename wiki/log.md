# Log

Append-only. One entry per ingest, newest at the bottom, consistent
prefix: `## [YYYY-MM-DD] kind | Short title` where kind is `feature`,
`fix`, `decision`, `content`, or `lint`.

## [YYYY-MM-DD] decision | Project started from template
- Renamed project, set base resolution, adjusted layers and input map
- Seeded design.md with pitch and pillars
